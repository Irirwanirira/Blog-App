<%= link_to user_path(user) do %>
    <div>
      <img src="<%= user.photo %>" alt="image">
    </div>
      
    <div>
        <h2><%= user.name %></h2>
        <p>
            <span>Number of posts: </span>
            <%= user.posts_counter || 0 %>
        </p>
    </div>
<% end %>